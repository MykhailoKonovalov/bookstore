<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a href="{{ path('home') }}" class="navbar-brand">BookStore</a>
        <div class="row">
            <div class="col-6">
                <a href="{{ path('book_list') }}" class="btn btn-secondary">
                    Каталог
                </a>
            </div>
        </div>
        {% include('components/searchForm.html.twig') %}
        {{ render_esi(url('user_block')) }}
        {% include('components/basket.html.twig') %}
    </div>
</nav>